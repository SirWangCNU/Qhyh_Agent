# 验证清单

## 后端持久化修复
- [ ] 后端 chat_stream 在 conversation_id 为空时自动创建新会话
- [ ] 后端 chat_sync 在 conversation_id 为空时自动创建新会话
- [ ] SSE done 事件 data 中始终包含 conversation_id（无论前端是否传入）
- [ ] 自动创建的会话标题为首条用户消息前30字
- [ ] 自动创建的会话消息正确落库（user + assistant 两条）
- [ ] 后端 pytest 全部通过（167+ tests）

## 前端闭包 bug 修复
- [ ] useConversation 使用 conversationIdRef 避免闭包陷阱
- [ ] sendMessage 读取 ref 中的 conversationId 而非闭包值
- [ ] SSE done 事件中的 conversation_id 能正确回写到前端状态
- [ ] ChatPage 移除预创建会话逻辑，直接发送消息
- [ ] conversationId 从 null 变为有值时 URL 自动更新
- [ ] SidebarNewPlan 改为直接导航到 /chat（或保持预创建但流程正确）
- [ ] npx tsc --noEmit 无类型错误

## 侧边栏视觉重构
- [ ] Sidebar 背景色调整为温暖奶油色调，符合杂志风格
- [ ] 顶部没有突兀的大图标/空白区域
- [ ] SidebarHeader 折叠按钮样式精致、不突兀
- [ ] SidebarNewPlan "新建对话"按钮样式精致（圆角、hover 效果、字体适中）
- [ ] 对话历史列表项样式美观（首字圆形图标、标题、时间层次分明）
- [ ] 工坊记录列表项样式与对话历史统一
- [ ] 选中项有明显的品牌色高亮
- [ ] hover 时有微妙的背景变化和缩放效果
- [ ] 两个分组（对话历史/工坊记录）之间有优雅的分隔
- [ ] 空状态提示友好（不是简单的灰色"暂无对话"）

## 对话历史删除功能
- [ ] 对话历史列表项 hover 时显示红色删除按钮
- [ ] 点击删除弹出确认框
- [ ] 确认后记录被删除，列表实时更新
- [ ] 删除当前活跃会话时跳转到空对话页

## 折叠状态
- [ ] 折叠状态（64px）下只显示首字圆形图标
- [ ] 折叠状态下新建按钮只显示 + 图标
- [ ] 折叠状态下点击正常工作

## 端到端流程
- [ ] 新对话发送消息后，侧边栏实时出现新记录
- [ ] 多轮对话消息正确追加到同一会话
- [ ] 刷新页面后历史记录仍然存在
- [ ] 点击历史记录能正确加载消息
- [ ] 工坊记录原有功能（点击跳转、删除、进度）正常
